Feature: Confirm nextjs app builds and loads correctly

  Scenario: Confirm nextjs app builds and loads correctly
    Given I open the 'NEXT_JS' app home page
    And I click the sign in button
    And I confirm provider is visible in iframe