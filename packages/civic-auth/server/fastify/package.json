{
  "name": "civic-auth-fastify-example",
  "version": "0.0.1",
  "description": "A sample fastify app with Civic Auth",
  "main": "src/app.ts",
  "type": "module",
  "private": "true",
  "scripts": {
    "start": "PORT=${PORT:-3003} bun src/app.ts",
    "dev": "PORT=${PORT:-3003} bun --watch src/app.ts",
    "build": "bun build src/app.ts --target bun"
  },
  "dependencies": {
    "@civic/auth": "*",
    "@fastify/cookie": "^11.0.1",
    "bun": "^1.2.5",
    "dotenv": "^16.4.7",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.6.3"
  }
}
