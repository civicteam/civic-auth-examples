{
  "name": "civic-auth-fastify-example",
  "version": "0.0.1",
  "description": "A sample fastify app with Civic Auth",
  "main": "dist/app.js",
  "type": "module",
  "private": "true",
  "scripts": {
    "start": "bun run src/app.ts",
    "build": "bun build src/app.ts --target bun",
    "dev": "PORT=${PORT:-3000} bun run src/app.ts --watch"
  },
  "dependencies": {
    "@civic/auth": "^0.7.0",
    "@fastify/cookie": "^11.0.1",
    "bun": "^1.2.5",
    "dotenv": "^16.4.7",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.6.3"
  }
}
